
            // const file = await fsp.readFile(files.file.path, 'utf-8');
            // let rows = file.split("\n");
            // // console.log("---------------ROWS---------------------");
            // const jsonObj = [];
            // let firstRow = rows[0].split(',');
            // let columns = file.split(",");

            // // console.log(columns);

            // for (let i = 0; i < rows.length; i++) {
            //     // console.log(rows[i]);
            //     let row = rows[i].split(',');
            //     let object = new Object();
            //     for (let j = 0; j < firstRow.length; j++) {
            //         // map1.set(columns[j], rows[i][j]);
            //         object[columns[j]] = row[j][i];
            //     }
            //     jsonObj.push(object);
            // }
